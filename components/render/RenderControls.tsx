"use client";import {useEffect,useState} from "react";import type {RenderJob,RenderMode} from "@/lib/render/render-jobs";export const RenderControls=({projectId}:{projectId:string})=>{const[job,setJob]=useState<RenderJob|null>(null);const[error,setError]=useState<string|null>(null);const start=async(mode:RenderMode)=>{setError(null);const response=await fetch(`/api/projects/${encodeURIComponent(projectId)}/renders`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({mode})});const data=await response.json() as {job?:RenderJob;error?:string};if(!response.ok||!data.job){setError(data.error||"Unable to start render");return;}setJob(data.job);};useEffect(()=>{if(!job||job.status==="completed"||job.status==="failed")return;const timer=window.setInterval(async()=>{const response=await fetch(`/api/renders/${job.id}`,{cache:"no-store"});const data=await response.json() as {job?:RenderJob};if(data.job)setJob(data.job);},700);return()=>window.clearInterval(timer);},[job]);return <div className="render-controls"><button disabled={job?.status==="running"} onClick={()=>void start("final")}>Export MP4</button><button disabled={job?.status==="running"} onClick={()=>void start("overlay")}>Overlay WebM</button>{job?<span className={`render-status ${job.status}`}>{job.status} · {Math.round(job.progress*100)}%</span>:null}{job?.status==="completed"?<a href={`/api/renders/${job.id}/output`}>Download</a>:null}{job?.status==="failed"?<button onClick={()=>void start(job.mode)}>Retry</button>:null}{error?<span className="render-error">{error}</span>:null}</div>};
