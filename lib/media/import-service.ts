import {extname} from "node:path";import {randomUUID} from "node:crypto";import type {FileSystemAdapter,FfmpegAdapter} from "@/adapters/contracts";import {applyProjectCommand} from "@/lib/project/commands";import type {ProjectRepository} from "@/lib/project/repository";import type {Asset} from "@/schemas/asset";import type {Project} from "@/schemas/project";import {captionsToFrames,parseSubtitleText} from "@/lib/captions/parser";const SAFE_FILE_NAME=/[^A-Za-z0-9._-]+/g;const sanitizeFileName=(name:string)=>{const n=name.replaceAll("\\","/").split("/").pop()||"asset";return n.replace(SAFE_FILE_NAME,"-").replace(/^-+|-+$/g,"")||"asset";};const mediaKind=(fileName:string):"video"|"subtitle"=>{const e=extname(fileName).toLowerCase();if(e===".mp4")return"video";if(e===".srt"||e===".vtt")return"subtitle";throw new Error("Supported Phase 5 imports: MP4, SRT and VTT.");};export type ImportMediaInput={projectId:string;fileName:string;mimeType?:string;bytes:Uint8Array};export class MediaImportService{constructor(private readonly fs:FileSystemAdapter,private readonly ffmpeg:FfmpegAdapter,private readonly repository:ProjectRepository,private readonly idFactory:()=>string=randomUUID){}async importFile(input:ImportMediaInput):Promise<Project>{let project=await this.repository.load(input.projectId);const kind=mediaKind(input.fileName);const assetId=`media-${this.idFactory()}`;const safeName=sanitizeFileName(input.fileName);const relativePath=`${kind==="subtitle"?"captions":"input"}/${assetId}-${safeName}`;const absolutePath=this.repository.resolveProjectFile(project.project.id,relativePath);await this.fs.writeBinary(absolutePath,input.bytes);let asset:Asset={id:assetId,kind,relativePath,label:safeName,originalName:input.fileName,mimeType:input.mimeType||(kind==="video"?"video/mp4":input.fileName.toLowerCase().endsWith(".vtt")?"text/vtt":"application/x-subrip"),sizeBytes:input.bytes.byteLength};if(kind==="video"){let probe;try{probe=await this.ffmpeg.probe(absolutePath);}catch(error){throw new Error(`Unable to read imported MP4 metadata with ffprobe: ${error instanceof Error?error.message:String(error)}. Verify the file and retry.`);}const durationInFrames=Math.max(1,Math.round(probe.durationSeconds*project.canvas.fps));asset={...asset,durationInFrames,width:probe.width,height:probe.height,sourceFps:probe.fps,hasAudio:probe.hasAudio};for(const clip of project.tracks.find(t=>t.id==="video-main")?.clips??[])project=applyProjectCommand(project,{type:"remove-clip",clipId:clip.id});project=applyProjectCommand(project,{type:"set-duration",durationInFrames});project=applyProjectCommand(project,{type:"add-asset",asset});project=applyProjectCommand(project,{type:"add-clip",trackId:"video-main",clip:{id:`clip-${assetId}`,type:"video",assetId,startFrame:0,durationInFrames,sourceStartFrame:0,volume:1,enabled:true,layer:0}});}else{const parsed=parseSubtitleText(new TextDecoder().decode(input.bytes));if(parsed.length===0)throw new Error("No valid subtitle cues were found. Verify SRT/VTT timestamps and retry.");for(const clip of project.tracks.find(t=>t.id==="captions-main")?.clips??[])project=applyProjectCommand(project,{type:"remove-clip",clipId:clip.id});project=applyProjectCommand(project,{type:"add-asset",asset});for(const caption of captionsToFrames(parsed,project.canvas.fps))project=applyProjectCommand(project,{type:"add-clip",trackId:"captions-main",clip:{...caption,id:`${assetId}-${caption.id}`}});}await this.repository.save(project);return project;}}
