import {randomUUID} from "node:crypto";import type {RemotionRenderAdapter} from "@/adapters/contracts";import type {ProjectRepository} from "@/lib/project/repository";export type RenderMode="final"|"overlay";export type RenderJob={id:string;projectId:string;mode:RenderMode;status:"queued"|"running"|"completed"|"failed";progress:number;outputRelativePath?:string;error?:string;createdAt:string;updatedAt:string};export class RenderJobManager{private readonly jobs=new Map<string,RenderJob>();constructor(private readonly adapter:RemotionRenderAdapter,private readonly repository:ProjectRepository){}create(projectId:string,mode:RenderMode,assetBaseUrl:string){const id=randomUUID();const now=new Date().toISOString();const job:RenderJob={id,projectId,mode,status:"queued",progress:0,createdAt:now,updatedAt:now};this.jobs.set(id,job);void this.execute(job,assetBaseUrl);return job;}get(id:string){return this.jobs.get(id)??null;}private patch(id:string,patch:Partial<RenderJob>){const current=this.jobs.get(id);if(!current)return;this.jobs.set(id,{...current,...patch,updatedAt:new Date().toISOString()});}private async execute(job:RenderJob,assetBaseUrl:string){this.patch(job.id,{status:"running",progress:.1});try{const project=await this.repository.load(job.projectId);const ext=job.mode==="overlay"?"webm":"mp4";const relativePath=`render/${job.mode}-${job.id}.${ext}`;const outputPath=this.repository.resolveProjectFile(job.projectId,relativePath);this.patch(job.id,{progress:.2,outputRelativePath:relativePath});await this.adapter.render({project,outputPath,mode:job.mode,assetBaseUrl});this.patch(job.id,{status:"completed",progress:1});}catch(error){this.patch(job.id,{status:"failed",progress:0,error:error instanceof Error?error.message:String(error)});}}}
