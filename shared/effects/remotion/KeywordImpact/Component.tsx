import {interpolate,useCurrentFrame} from "remotion";import {KeywordImpactPropsSchema} from "./schema";import type {EffectComponentProps} from "../../types";export const KeywordImpact=({props,motionSpeed=1}:EffectComponentProps)=>{const p=KeywordImpactPropsSchema.parse(props);const f=useCurrentFrame()*motionSpeed;const x=interpolate(f,[0,12],[-120,0],{extrapolateRight:"clamp"});return <div style={{position:"absolute",left:p.align==="center"?"50%":"7%",top:"42%",transform:p.align==="center"?`translateX(calc(-50% + ${x}px))`:`translateX(${x}px)`,fontSize:94,fontWeight:950,lineHeight:1,padding:"18px 24px",background:"rgba(8,11,15,.82)",borderLeft:`12px solid ${p.accentColor}`,color:"white",whiteSpace:"nowrap"}}>{p.text}</div>};
