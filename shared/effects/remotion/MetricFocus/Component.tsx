import {interpolate,useCurrentFrame} from "remotion";import {MetricFocusPropsSchema} from "./schema";import type {EffectComponentProps} from "../../types";
export const MetricFocus=({props}:EffectComponentProps)=>{const p=MetricFocusPropsSchema.parse(props);const f=useCurrentFrame();const width=interpolate(f,[0,30],[0,100],{extrapolateRight:"clamp"});return <div style={{position:"absolute",left:"8%",right:"8%",bottom:"18%",padding:34,borderRadius:28,background:"rgba(10,16,22,.86)",border:`2px solid ${p.accentColor}`}}><div style={{fontSize:28,color:"#aeb9c5",letterSpacing:3}}>{p.title}</div><div style={{fontSize:120,fontWeight:950,color:"white"}}>{p.value}<span style={{fontSize:50,color:p.accentColor}}>{p.unit}</span></div><div style={{height:16,borderRadius:9,background:"#28323c",overflow:"hidden"}}><div style={{height:"100%",width:`${width}%`,background:p.accentColor}}/></div></div>};
