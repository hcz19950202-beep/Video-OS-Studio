import {spring,useCurrentFrame,useVideoConfig} from "remotion";import {LowerThirdPropsSchema} from "./schema";import type {EffectComponentProps} from "../../types";
export const LowerThird=({props}:EffectComponentProps)=>{const p=LowerThirdPropsSchema.parse(props);const f=useCurrentFrame();const{fps}=useVideoConfig();const enter=spring({frame:f,fps,config:{damping:18}});return <div style={{position:"absolute",left:"6%",bottom:"9%",transform:`translateX(${(1-enter)*-100}px)`,padding:"20px 28px",minWidth:360,borderRadius:18,background:"rgba(8,11,15,.86)",borderLeft:`8px solid ${p.accentColor}`}}><div style={{fontSize:42,fontWeight:900,color:"white"}}>{p.name}</div><div style={{fontSize:25,color:p.accentColor,marginTop:6}}>{p.role}</div></div>};
