import {interpolate,spring,useCurrentFrame,useVideoConfig} from "remotion";import {BigNumberPropsSchema} from "./schema";import type {EffectComponentProps} from "../../types";export const BigNumber=({props}:EffectComponentProps)=>{const p=BigNumberPropsSchema.parse(props);const frame=useCurrentFrame();const{fps}=useVideoConfig();const progress=spring({frame,fps,config:{damping:15}});const transform=p.animationStyle==="slide"?`translateY(${(1-progress)*70}px)`:`scale(${progress})`;const opacity=interpolate(frame,[0,8],[0,1],{extrapolateRight:"clamp"});return <div style={{position:"absolute",left:"8%",top:"22%",transform,transformOrigin:"left center",opacity}}><div style={{fontSize:30,fontWeight:800,letterSpacing:4,color:p.accentColor}}>{p.label}</div><div style={{fontSize:p.fontSize,lineHeight:.9,fontWeight:950,color:"white"}}>{p.value}<span style={{fontSize:48,color:p.accentColor,marginLeft:16}}>{p.suffix}</span></div></div>};
