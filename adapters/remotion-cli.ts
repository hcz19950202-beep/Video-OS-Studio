import {execFile} from "node:child_process";import {writeFile,mkdir} from "node:fs/promises";import {dirname,join} from "node:path";import {promisify} from "node:util";import type {RemotionRenderAdapter} from "@/adapters/contracts";const execFileAsync=promisify(execFile);const defaultCli=()=>process.env.REMOTION_CLI_PATH||join(process.cwd(),"node_modules",".bin",process.platform==="win32"?"remotion.cmd":"remotion");export class NodeRemotionCliAdapter implements RemotionRenderAdapter{constructor(private readonly cliPath=defaultCli(),private readonly entryPoint=join(process.cwd(),"remotion","index.ts")){}async render({project,outputPath,mode,assetBaseUrl}:Parameters<RemotionRenderAdapter["render"]>[0]){await mkdir(dirname(outputPath),{recursive:true});const propsPath=`${outputPath}.props.json`;const assetUrls=Object.fromEntries(project.assets.map(a=>[a.id,`${assetBaseUrl}/api/projects/${encodeURIComponent(project.project.id)}/assets/${encodeURIComponent(a.id)}`]));await writeFile(propsPath,JSON.stringify({project,assetUrls,renderMode:mode}),"utf8");const args=[this.entryPoint,"VideoOSMaster",outputPath,"--props",propsPath,"--width",String(project.canvas.width),"--height",String(project.canvas.height),"--fps",String(project.canvas.fps),"--duration",String(project.canvas.durationInFrames),"--overwrite"];if(mode==="overlay")args.push("--image-format=png","--pixel-format=yuva420p","--codec=vp8");else args.push("--codec=h264");try{await execFileAsync(this.cliPath,args,{windowsHide:true,maxBuffer:20*1024*1024,env:process.env});}catch(error){const message=error instanceof Error?error.message:String(error);throw new Error(`Remotion render failed: ${message}. Ensure @remotion/cli is installed at the matching Remotion version or set REMOTION_CLI_PATH, then retry.`);}return{outputPath};}}
