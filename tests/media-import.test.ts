import {describe,expect,it} from "vitest";import type {FfmpegAdapter} from "@/adapters/contracts";import {InMemoryFileSystemAdapter} from "@/adapters/filesystem";import {MediaImportService} from "@/lib/media/import-service";import {ProjectRepository} from "@/lib/project/repository";const ffmpeg:FfmpegAdapter={probe:async()=>({durationSeconds:4,width:1080,height:1920,fps:30,hasAudio:true}),waveformPeaks:async(_path:string,points:number)=>Array(points).fill(.5),normalizeVideo:async input=>({outputPath:input.outputPath}),normalizeAudio:async input=>({outputPath:input.outputPath})};describe("MediaImportService",()=>{it("imports an MP4 with metadata and primary video clip",async()=>{const fs=new InMemoryFileSystemAdapter();const repo=new ProjectRepository(fs,"/data");await repo.create({id:"demo",name:"Demo",durationInFrames:30});const service=new MediaImportService(fs,ffmpeg,repo,()=>"fixed-id");const p=await service.importFile({projectId:"demo",fileName:"talking head.mp4",mimeType:"video/mp4",bytes:new Uint8Array([1,2,3])});expect(p.canvas.durationInFrames).toBe(120);expect(p.assets[0].relativePath).toBe("input/media-fixed-id-talking-head.mp4");expect(p.tracks.find(t=>t.id==="video-main")?.clips).toHaveLength(1);});it("imports and parses SRT into caption clips",async()=>{const fs=new InMemoryFileSystemAdapter();const repo=new ProjectRepository(fs,"/data");await repo.create({id:"demo",name:"Demo",fps:30});const service=new MediaImportService(fs,ffmpeg,repo,()=>"subtitle-id");const p=await service.importFile({projectId:"demo",fileName:"captions.srt",bytes:new TextEncoder().encode("1\n00:00:00,000 --> 00:00:01,000\nHello 90%")});expect(p.assets[0]).toMatchObject({kind:"subtitle",relativePath:"captions/media-subtitle-id-captions.srt"});expect(p.tracks.find(t=>t.id==="captions-main")?.clips[0]).toMatchObject({type:"caption",text:"Hello 90%",durationInFrames:30});});});
