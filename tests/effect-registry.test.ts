import {describe,expect,it} from "vitest";import {EFFECT_REGISTRY,EFFECTS_BY_ID} from "@/shared/effects/registry";
describe("Phase 3 effect registry",()=>{it("registers exactly the four initial Remotion effects",()=>{expect(EFFECT_REGISTRY.map((e)=>e.id)).toEqual(["big-number","metric-focus","keyword-impact","lower-third"]);expect(EFFECT_REGISTRY.every((e)=>e.engine==="remotion")).toBe(true);});it("validates every default props object through its own schema",()=>{for(const effect of EFFECT_REGISTRY)expect(()=>effect.schema.parse(effect.defaults)).not.toThrow();expect(EFFECTS_BY_ID["big-number"].thumbnail).toBe("/effects/big-number.svg");});});
